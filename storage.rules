rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Fotos de triagem não são persistidas. Uploads ficam bloqueados por padrão.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
